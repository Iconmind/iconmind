name: iconmind_flutter_example
description: Gallery demo for iconmind_flutter — variants, weights, sizing and theming.
publish_to: none
version: 0.1.0

environment:
  sdk: ">=3.3.0 <4.0.0"
  flutter: ">=3.16.0"

dependencies:
  flutter:
    sdk: flutter
  iconmind_flutter:
    path: ../
